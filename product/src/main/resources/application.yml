spring:
  application:
    name: product-service

  datasource:
    url: jdbc:h2:mem:test

  h2:
    console:
      enabled: true

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

server:
  port: 8081